# OS / エディタの生成物
.DS_Store
Thumbs.db
desktop.ini

# 一時ファイル・ログ
*.tmp
*.log

# 依存(本作は依存ゼロだが念のため)
node_modules/

# デザインループの検証スクショ(devtool撮影の使い捨て。履歴に載せない)
docs/mocks/**/*.png

# Claude Codeのローカル設定
.claude/

# デザイン検証の使い捨てモックHTML（残したい場合は docs/mocks/ へ）
web/*mock*.html
web/trail-*.html
web/*-compare.html

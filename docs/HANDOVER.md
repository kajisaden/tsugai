# 二間詰 引き継ぎ HANDOVER.md

> このファイルはAI(Claude Code)向けの作業引き継ぎです。
> 設計の土台は **SPEC.md** に全部あります。まず SPEC.md を読んでから本書のタスクに入ってください。
> 本書は短命です(作業が進むたびに古くなる)。SPEC.md が安定仕様、本書が「次に何をするか」。
>
> **現行前提(2026-07-01 時点・コードが正)**: アプリは**ライト(washi)専用**(ダークテーマ/テーマトグルは廃止)。ホーム画面は**ホームハブ**(エンブレム＋レベル番号＋プレイ／通常・上級モードタブ／デイリー・一覧・目標・ストアのフッタ)に刷新され、**レベルロック/解放**(`nikenzume.levelLock.v1`/`.unlocked.v1`)・**デイリーチャレンジ**(`nikenzume.daily.v1`)を搭載。通常モードのクリアはホームハブへ戻り次レベルへ自動スライド、A画面(切れ目カード)はデイリー専用。以下の日付つき過去エントリでダーク/テーマ切替/旧クリアフローを「現行」として書いている箇所は履歴として読むこと。

---

## 現在地 (2026-06-19 更新) — UI大改修(一覧/操作系/ヘッダ/設定)。全て公開済み(HEAD `2044a78`)

このセッションは「速いデザインループ」で UI を一通り作り直した。**全コミット push 済み・Pages 反映検証済み**。`docs/mocks/loop/` の検証スクショは未追跡(従来通り)。デプロイは毎回 `node tools/stamp-cache.mjs`→commit→push→Pages一致をcurl grep検証。

### このセッションでやったこと(公開済み)
- **一覧カード=盤色化**: 問題タイル `.level-tile`・A画面 `.gap-card`・章カード `.chapter-card`(ダーク) を **盤床と同じ radial `#32323f→#1e1e27`** に統一(壁色より暗い)。ライトは washi/紙のまま。
- **クリア演出の幕撤去**: `#overlay-tsumi`(クリア時に文字を薄くするベール) を完全撤去。クリア→発光+弾み約1秒。**※その後の遷移先はモードで分岐(後日追加): 通常モード=ホームハブへ戻りクリアフィードバック→次レベルへ自動スライド(`showHomeClearFeedback`/`startHomeSlide`)、A画面(切れ目カード `#overlay-gap`/`goToGap`)はデイリーモード専用。**
- **アプリアイコン刷新**: A画面の重なり2球(案C=金球+盤の灯り背景)に。`web/{apple-touch-icon,icon-192,icon-512}.png` 差し替え(canvasでなく `docs/mocks/icon-render.html` を実描画→screenshot生成)。
- **ダークのカード外薄字を明るく**: `--text-dim` ダーク `#6f6c64→#9d9a8e`(戻る/操作系/ヒント文等)。ライトは不変。
- **一覧クリア印=A画面エンブレム化**: 単球 `.lv-ball` → 2球の `.lv-emblem`(220×176座標を `scale(0.16)` で縮小。ダーク=発光球/ライト=碁石+金銀皿+後光)。最短=金/クリア=白(銀)。
- **操作系の刷新(プレイ画面)**: 待った(undo)**完全撤去**。初形→**最初から**。盤下 `#hint-row`=**最初から / 光ヒント**、**答え**はヘッダ(`#play-status`)へ移動。**次の手ヒントは UI 撤去**(`showNextMove`/`hintFromState` は関数のみ互換保持)。手数「○手」は戻ると同サイズ(16px)。
- **ヒント=ストック制(`nikenzume.hints.v2`、light/next/answer の3種)**: 光ヒント/答えは `spendHint` で残数を1消費し、**残数0でリワード広告**(`watchRewardAd`)。残数はログインボーナス/デイリーで補充(日次リセットではない)。答えは固有の `answer` クレジット。コイン無し。
- **ヘッダ再構成**: ヘッダ左=**ホームボタン(家型SVGアイコン・`#btn-home`・32px丸・明色暖色グラデ #fff8eb→#e8d8c4)**。押すと文脈を保ってホームハブ(`view-chapters`)へ戻る(`goHomeFromHeader`)。「‹戻る」シェブロン・テーマトグルは**廃止**(ダークモード廃止に伴う)。ヘッダ=ホーム(左)/タイトル＋答え(中央 `#play-status`)/設定歯車(右)。ヘッダ全幅化(`width:100%`)で左右マージンに整列。
- **設定=右サイドシート(ドロワー)**: 歯車で開く/×・スクリム・Escで閉じる。ダーク背景=A画面カード色、ライト=紙。`#settings-drawer`。
- **設定の中身(全項目)**: 表示(言語のみ。テーマ切替は廃止) / 音・操作感(効果音/触覚) / データ(進捗をリセット) / 情報(遊び方/バージョン履歴/プライバシー/問い合わせ/統計・実績/レビュー) / 購入(広告を消す/復元)。
- **一覧の上余白詰め**: 章一覧/問題一覧を `main:has(...)` で上寄せ(`flex-start`)＋ `#view-chapters,#view-levels{margin-top:60px}`(ロゴ↔先頭 ≈68px)。プレイ画面は対象外。

### ★ 未実装 / プレースホルダ(次にやること)
- **設定の真の未実装は3項目のみ(`data-soon`→「準備中」トースト)**: レビューする・広告を消す・購入を復元。遊び方(`#howto-overlay`)・バージョン履歴(v1.0.0)・プライバシーポリシー(`#privacy-overlay`)・統計/実績(`#stats-overlay`)は専用オーバーレイで**実装済み**、お問い合わせも外部リンクで**実装済み**(ただし **Googleフォーム URL は仮置き `https://forms.gle/PLACEHOLDER`** = 差替要)。「このアプリについて(About)」項目はUIから削除済み(`about` 文言のみ残置・未使用)。
- **効果音(SE)・触覚フィードバック**: **本体実装済み**(SE=Web Audio 合成 `playMove/playBump/playClear/playFoul`、触覚=Capacitor Haptics＋`navigator.vibrate` フォールバック `haptic()`、設定で既定ON・`nikenzume.se.v1`/`.haptics.v1`)。残=`playTap`(UIタップ音)は定義のみ未配線、実機(iOS)での触覚確認・音色の磨き込み。
- **広告SDK**: **AdMob(Capacitor)接続済み**。`initAdMob`/`showInterstitial`/`watchRewardAd` 実装、リワード=ヒント残数0時(光ヒント/答え=`spendHint`)、インタースティシャル=切れ目(`goToGap`)。**adId は Google 公式テストID=本番前に実IDへ差替**。PWA/ブラウザでは AdMob 未ロードで即時付与にフォールバック。プライバシーポリシーは広告配信に必須。
- **3部屋ビューア(積み残し)**: `web/pool-3room.js`(2589問・データ完成済み)の縦並び3盤統合。盤サイズ計算の除数 `/2`→`/3`、`--play-chrome` 再調整(現在 276。トグルは設定へ移ったのでヘッダは一段)。
- **実機確認(iPhone standalone)**: 本セッションの変更一式(特にダーク半輝度・設定ドロワーのスライド/セーフエリア・背の低い端末での盤と操作系の収まり)。

### 主要セレクタ/キー(参照)
- localStorage: `nikenzume.cleared.v1`/`.best.v1`(進捗・リセット対象)、`.lang.v1`、`.hints.v2`(残数)、`.login.v1`(ログインボーナス)、`.daily.v1`(デイリー)、`.levelLock.v1`/`.unlocked.v1`(レベルロック/解放)、`.se.v1`/`.haptics.v1`。`.theme.v1` はダーク廃止で未使用。
- i18n: `locale`(可変)/`t()`/`fillI18n()`(静的)+ `relocalize()`(言語切替で静的再翻訳＋現ビュー再描画)。`strings.js` に ja/en。
- 設定: `#settings-drawer`(`.open` で表示)/`openSettings`/`closeSettings`/`resetProgress`/`updateSettingsUI`。

---

## 現在地 (2026-06-17 更新)

- **壁カードの統一(一覧タイル＋A画面カード)(2026-06-17)**: ゲーム盤の壁・一覧レベルタイル・A画面カードを同一の「壁から彫り出したカード」に統一。`web/style.css` 反映済み:
  - **一覧 `.level-tile`**: 従来の `button` 由来の囲みブロック → 壁カード(ダーク=持ち上げ壁色 `#4a4a57→#3a3a45` ＋ `inset 0 1px 0 rgba(255,255,255,.1)` ＋ 角丸14px / ライト=washi `#ddd5c4→#cfc6b2`)。クリア済みの金縁(`.level-tile.cleared`)・球は specificity で維持。
  - **A画面 `.gap-card`**: ダークを `#2e2e3a→#22222c` → `#4a4a57→#3a3a45` に持ち上げ統一(低輝度対応で盤の壁だけ持ち上げA画面カードがズレていたのを解消)。ライトは元から washi(`#ddd5c4→#cfc6b2`)で不変。
  - これで **盤の壁・一覧タイル・A画面カードが同色**。chrome-devtools 390×844 で両テーマ＋擬似50%(`filter: brightness(.5)`)検証。stamp(`style.css?v=f66938c2ec`)。**残: 実機確認**。
  - 関連メモ: 低輝度設計方針は [[tsugai-dark-theme-brightness]]。

- **ダーク盤の視認性改善(床＋壁)(2026-06-17)**: 「ダークの盤が見えづらい」を床と壁の2段で解消(速いデザインループ)。`web/style.css` 反映済み(コミット済・push 未):
  - **床**: `.board` 既定(ダーク)背景 `#121218`(地色#0a0a0e〜#101018と同化し沈む)→ `radial-gradient(120% 120% at 50% 42%, #24242f 0%, #16161d 72%)`(中央を灯したグラデ=面に陰影)。ライトは `:root[data-theme="light"] .board` に `background: var(--panel)` を追加し紙色を維持。**`--panel` 変数自体は不変**(ボタン背景 line 92 にも使うので波及させない)。
  - **壁**: 床を上げると壁(従来#2e2e3a→#22222c≈中間#28)が床中央#24に近づき沈むため、ダーク `.cell.wall` 背景を `#3a3a47→#2c2c37` に一段持ち上げ。**スレートの色みは維持**しA画面カード(`.gap-card`=元の壁色)と色を揃えた(青み増しの案は不採用)。ライト壁は無変化。
  - 検証: chrome-devtools 390×844 で両テーマ・実puzzle/仮設壁で目視＋computed style(ライト床#fbf8f1・ライト壁#ddd5c4 が無変化を確認)。stamp-cache(`style.css?v=ad31923379`)。**この段は push 済み・Pages 反映検証済み・実機確認済み**。
  - **低輝度再チューニング(2026-06-17 追)**: 実機で「画面輝度を最大にすると良いが、50%程度だと盤が見えづらい」判明。**設計方針=最大輝度でなく常用50%を基準にし、低輝度でも崩れない“コントラスト”を確保**(暗所/くつろぎ環境で遊ばれやすい)。ダークの段差を拡大: 床 `#24242f→#16161d` → **`#32323f→#1e1e27`**、壁 `#3a3a47→#2c2c37` → **`#4a4a57→#3a3a45`**。地色(ページ背景)とライトは不変=**盤のみ強化**(地色も沈める案3より、盤のみの案2を採用。最大輝度ではやや明るめになるが視認性優先)。検証は chrome-devtools で `filter: brightness(.5)` の擬似低輝度プレビュー＋full。stamp(`style.css?v=c9ef01a29b`)。**残: 実機で半分輝度を再確認**。

- **ゲーム画面(プレイ画面)の盤デザイン微修正(2026-06-17)**: 「速いデザインループ」(提案→モック→実装)で2点、`web/style.css` に反映済み(コミット済・push 未):
  - **盤の枠=案A 額装インセット**: `.board` を少しへこんだ盆として枠取りし「部屋としてのまとまり」を出す(地色との差が小さく輪郭が弱かった)。既定(ダーク)=`border 1px rgba(255,255,255,.11)` ＋ `box-shadow: inset 0 2px 9px rgba(0,0,0,.55)`、ライト(`:root[data-theme="light"] .board`)=`border rgba(43,38,32,.20)` ＋ 暖色の弱い影 `inset 0 2px 7px rgba(70,55,30,.12), 0 1px 2px rgba(70,55,30,.06)`。格子線(`--line` / `.cell` border)は不変。
  - **壁の継ぎ目=案2 単線**: `.cell.wall` の上辺ハイライト(`inset 0 1px 0` ダーク.1 / ライト.5)を**両テーマで廃止**。壁マス毎にハイライトが出ると、隣接壁の継ぎ目で境界線と二重になり線が「浮く」(実機ダークで顕著)→ 継ぎ目は境界線1本に。`.gap-card`(A画面カード)は別セレクタなので無影響。
  - 検証: chrome-devtools 390×844 で両テーマ＋仮設壁ブロックで目視＋computed style。stamp-cache 済み(`style.css?v=5cc4cface6`)。**残: 実機確認 → push → Pages 反映検証**。他候補(ボールをライトで磨き碁石に揃える / ゴールの見せ方)は未着手。

- **ライト A画面エンブレムの質感改善＝案C 実装(2026-06-17)**: ライトが「鮮やかな金皿＋フラット黒球=玉子焼き/絵文字っぽい」件を解消。**案C**を `web/style.css` light セクション＋`web/index.html` に反映済み(コミット済・**push 未**):
  - **ボール=磨いた碁石**: `:root[data-theme="light"] #gap-emblem .emblem-ball` を `radial-gradient(circle at 35% 26%, #6c6c77 0%, #37373f 30%, #1b1b21 68%, #0d0d11 100%)` ＋ `box-shadow: inset -2px -3px 7px rgba(0,0,0,.6), inset 3px 3px 6px rgba(255,255,255,.16), 0 5px 9px rgba(40,32,16,.42)`(上面に反射のある立体)。
  - **金皿(最短)=漆金精緻化(ツヤ維持)**: `radial-gradient(120% 150% at 40% 24%, #f7ecc8 0%, #e0c585 40%, #bd9c52 72%, #8f7032 100%)` ＋ 磨きハイライト＋細い金縁(`inset 0 0 0 1px rgba(255,242,205,.18)`)。**マット化3案は実機エミュ比較の上ツヤ維持を選択**(マットは平板/黄身寄りに振れた)。
  - **銀皿(クリア)=金と同構造の精緻化ツヤ銀**で統一: `radial-gradient(120% 150% at 40% 24%, #f7fafd 0%, #d4dae3 40%, #aeb6c3 72%, #79808d 100%)`。
  - **後光halo**: `#gap-emblem` 先頭に `.emblem-halo` 要素を追加し、状態別に出し分け(最短=金 `rgba(216,183,95,..)` / クリア=銀 `rgba(185,192,204,..)`、`closest-side` ＋ `filter: blur(3px)`)。既定は `display:none`、`:root[data-theme="light"] #gap-emblem.best/.win .emblem-halo` でのみ表示。
  - **ダークは無傷**: halo/disc とも `display:none`(computed style 確認)、従来の発光球のまま。chrome-devtools 390×844 でライト best/win・ダーク回帰を目視＋計測。stamp-cache 済み(`style.css?v=2dcb6745d0`)。
  - **残**: 実機(iPhone standalone)確認 → 確定後に push → Pages ビルド一致＋ライブ反映を curl grep 検証。検証スクショは `docs/mocks/loop/`(未追跡)、設計モックは `docs/mocks/tsugai-{light-emblem,matte-disc}.html`。

## 現在地 (2026-06-15 更新)

- **クリア後の A画面(2026-06-16)**: クリア→盤で約1秒 発光＋弾み→**A画面(切れ目)へ自動遷移**(タップ/Enter で早送り)。A画面 = **盤の壁デザインを大きく角丸にしたカード**(`.gap-card`: ライト=washi壁 / ダーク=スレート壁＝`.cell.wall` と同配色＋角丸、`width: min(88vw,350px)` / `min-height: min(72vh,640px)`・中身は縦中央)の上に、上から: **手数**(`◯手でクリア`)＋最短(`最短は◯手`/`最短手数です`)→ **つがいエンブレム**(`#gap-emblem` 2球が約30°で寄り添う。ライト=碁石＋金(最短)/銀(クリア)の楕円皿、ダーク=金/銀の発光。`emblem-hop` で弾む。`.best`/`.win` で出し分け)→ **進捗**`第1章 X/20`→ ボタン**[次の問題へ/もう一度(常時表示)]**(カード幅にフィット。**壁カードから彫り出した raised 質感＋明朝体(ヒラギノ)**、次=金文字/もう一度=平文)。左上に**ホームアイコン**(=一覧へ、`#btn-list` を SVG 化)。文字色は**「案C入替」**: 見出し`◯手でクリア`と進捗の数字=**金**(`var(--gold)`)、`最短は◯手`/`第1章`/`/20`=**平文**(カードの中間トーンで沈むディムを廃止)。`overlay-tsumi` は手数表示を撤去し2秒ベール化。`もう一度`=同じ問題を初形から(`startPuzzle(cur)`)。`goToGap()` が `lastClear{moves,min,best}` で生成、`tsumiTimer` で2秒。reduced-motion は跳ね停止。strings に `retryAgain`(もう一度/Try again)。本コミットで公開。
- **ライト: 壁の印を金のにじみに(2026-06-16)**: ヒント/壁当ての印(`.hint-glow`/`.bump-glow`)はダークの発光を amber で流用しチープだった。ライトは「発光→滲み(水墨が紙に広がる)」へ転換=明るい金の芯 `#d9b65c` ＋ **オフセット無しの対称な金ハロー**(`box-shadow: 0 0 5px 1px / 0 0 11px 3px rgba(176,134,46,.5/.2)`)。印は縦横どちらの壁面にも出る(縦4x52/横52x4)ため、方向性のある影/グラデは縦横で不揃い・壁から浮いて見える→対称化で解決(ダークの対称発光に倣う)。明滅は浅い呼吸 `hint-glow-leaf`(opacity .82↔1)、reduced-motion で停止。公開済み(`7818f26`)。
- **盤の縦並びレイアウト(2026-06-15)**: スマホで2盤が横並び(各172px・画面下半分が空白)だったのを**縦1列・中央寄せ**に変更し、各盤を画面高に合わせて拡大(390×844 で262px=面積約2.3倍)。一辺は `min(100%, calc((100dvh - var(--play-chrome) - var(--board-gap)) / 2))` で算出し画面高に自動追従(背の低い端末でも1画面・スクロール無し)。`100vh` フォールバック＋`env(safe-area-inset-bottom)` 対応。盤間 `--board-gap: 44px`(別々の部屋と判別しやすく)、上下UI概算 `--play-chrome: 276px` を `:root` に変数化。`#boards` は `grid-template-columns: 1fr 1fr` → `1fr`。ダーク/ライト両テーマ・iPhone SE(375×667=174px)で chrome-devtools 目視確認 → stamp-cache → commit(`5cbce7e`) → push → Pages 一致をポーリング → curl でライブ反映(`style.css?v=85a7ffada3`)を grep 検証して公開済み。**将来の3部屋ビューアは除数(/2)と `--play-chrome` を別管理が必要**(下記タスク0参照)。
- **PWA化(ホーム画面で全画面テスト)(2026-06-15)**: ブラウザだと Safari のバーで上下が狭く本番(全画面)を再現できない件への対応。`web/index.html`＋root `index.html` に `apple-mobile-web-app-capable`/`mobile-web-app-capable`/`apple-mobile-web-app-status-bar-style=black-translucent`/`apple-touch-icon`/`theme-color` と `manifest.webmanifest`(`display:standalone`/portrait/`#0a0a0e`)を追加。アイコンは canvas 生成の二球(つがい)マーク(`web/{apple-touch-icon(180),icon-192,icon-512}.png`)。**iPhone Safari →共有→「ホーム画面に追加」で Safari バー無しの全画面起動**=`100dvh`/セーフエリア設計を本番条件で確認可能(ツールバー消失分さらに盤が大きく出る)。公開済み(`ba6369b`、Pages 一致＋manifest/アイコン 200 を curl 検証)。**注意: iOSは純PWAをApp Storeに出せない**=これはテスト/ホーム画面配布用。App Store本体は別途ネイティブ化が必要(下記タスク4=配信形態の決定)。**ステータスバー追補(`a7fa70d`)**: 初期 `=black` だと standalone で**白帯**が出てダーク基調と食い違ったため `black-translucent`(透過=本体背景が時計/電池の下まで広がりネイティブ風に溶け込む)へ。コンテンツが上に回るので `#app-header` の `padding-top` と `.board` の高さ計算に `env(safe-area-inset-top)` を加味(実機相当インセットで盤222px・スクロール無し・タイトルがバー回避を検証)。**ライト対応の試行→撤回(`d2ff3bc`→`c1900fc`)**: 当初「translucent は白文字固定」と想定しライト+standalone の上端にスクリム(`body::before`)を入れたが、**実機では iOS がステータス文字をシステムの明暗に追従させる(ライト時=黒文字)ため washi 上でも読め**、スクリムは不要な黒帯だった→撤回(削除)。`black-translucent` 自体は維持(白帯防止)。注意: システム明暗とアプリのテーマトグルが食い違う場合(例: システム明×アプリ暗)はステータス文字が読みにくくなり得る=将来必要なら再検討。**全画面の余白追補(`c293b69`)**: standalone(Safariツールバー無し)だと章一覧/問題一覧が上寄せで下に大きな余白が露出し「下が切れた」ように見えたため、`body` を縦フレックス・`main` をヘッダ下の残り高さ占有＋`justify-content: safe center` で**中身を縦中央寄せ**(`safe` で高すぎる時は上寄せ退避し上端非クリップ)。フレックスの auto マージンで縮まないよう `main { width:100% }` も明示。プレイ画面は従来どおり全画面充填(非破壊)。標準/standalone相当インセットの両方で3画面とも中央・全幅・非スクロール・非クリップを計測確認。**下端の白帯修正(`bbfd6a7`)**: `html`/`body` の `background-color` が透明で、地色は `body` のグラデ(箱内のみ)頼みだったため、実機 standalone のオーバースクロール/下端セーフエリアで根要素の白が透けていた(ライトで顕著)。`html { background-color: var(--bg) }` を敷き、テーマ地色(ライト=washi/ダーク=黒)で覆って解消。
- **ライトモード(washi)＋クリア演出の作り込み＋UX修正(2026-06-15)**: すべて chrome-devtools で目視確認→コミット→Pages ビルド一致＋ライブ反映を検証して公開済み。
  - **手動テーマトグル**: ヘッダ右上に太陽/月ボタン。`<html data-theme="light|dark">` で全切替・localStorage 永続(既定ダーク)・`<head>` の inline script で先行適用しチラつき防止。配色は `:root[data-theme="light"]` の CSS変数＋light スコープの上書きに集約(ダークは無傷)。
  - **ライト配色=生成り(washi)**: 紙地 `#f3efe6` / 墨字 `#2b2620` / 濃い琥珀 `#a8801f`。黒地の「発光」は紙で効かないので「発光→影」へ転換。
  - **ボール**: ライトは**マット黒の碁石**(立体陰影・静止)。ダークは従来の淡い発光球＝対(つがい)。
  - **クリア演出=金/銀メダリオン(案A)**: ぼかしグロー廃止。ゴールを球より大きい円盤(inset 5%)にして黒石を乗せる(最短=金箔 `#d8b75f` / クリア済み=銀 `#b9c0cc`)。ダークは従来の発光。
  - **クリアの弾み**: タップして次へ進むまで繰り返す(`.bouncing` を checkClear で付与・dismissTsumi/startPuzzle で除去)。動き `clear-bounce` =**拡大主体(scale 1.1)＋小さな跳ね(translateY -8%)**を両モード共通。ライトは接地影を別要素(`.ball-glow`)で床に残し反応(上がると縮み薄く)、ダークは発光が奥行き担当。reduced-motion で停止。
  - **失敗(反則)時**: 「一手戻す/初形へ」の2択を廃止し**初形へ戻すのみ**。文言は **初手から / Retry** のテキストボタン(リロードSVGアイコンは不採用)。
  - **ヒント**: 違う問題に移ると必ずオフから開始(同じ問題の初形/答え再生では保持)。localStorage 永続を廃し問題IDで管理。
  - **キャッシュバスティング**: GitHub Pages はヘッダ設定不可のため `tools/stamp-cache.mjs` で各アセット(css/js)の内容ハッシュを `web/index.html` の `?v=` に焼き込む。**デプロイ前(コミット前)に毎回 `node tools/stamp-cache.mjs` を実行する**。file:// では query は無視されローカル直開きも維持。
  - **切れ目の広告枠プレースホルダ(#ad-slot)の見える破線枠を削除**(差し込み口はHTMLコメントで残置)。
  - **デプロイ手順(踏襲)**: `node tools/stamp-cache.mjs` → commit(push は明示依頼時) → Pages ビルドが HEAD と一致するまでポーリング → `curl` でライブ反映を grep 検証。`gh api` は `MSYS_NO_PATHCONV=1 gh api repos/...`(先頭スラッシュ無し)。

## 現在地 (2026-06-14 更新)

- **文言の i18n 分離 + クリア演出刷新(2026-06-14)**: UI文言を `web/strings.js`(`window.NIKENZUME_STRINGS = {ja, en}`)に分離し、`t(key, params)` + HTMLの `data-i18n` 属性で流し込む(`app.js`/`index.html`)。fetch せず JS 埋め込み=`file://` 維持。ロケールは `navigator.language` 自動 + `?lang=en` 上書き(localStorage 永続)。英語の複数形は関数値で処理(`1 move`/`2 moves`)。**プロダクト名は "Tsugai"**(旧仮称 二間詰。docs本文の旧名は未一括置換=別途クリーンアップ)。**クリア演出を刷新**: 大文字「詰」オーバーレイを廃し、盤上のボールが**金(最短)/白(クリア)に発光**(`#boards.clear-best`/`.clear-win`、CSS)。言葉を出さない=クリアの climax を i18n から外す。手数だけ透過オーバーレイ下部に小さく添える。将棋用語は英語に持ち込まない(`6手詰`→`Solve in 6`)。**残: 言語切替の可視UI(現状は端末言語+URL)、docs本文の Tsugai 一括リネーム、3部屋ビューア**
- **3部屋プール生成(2026-06-14)**: `generate-pool.mjs --rooms 3 --seeds 1-6000`(4x4/5x5)で **2589問**生成。出力は本体を上書きしないよう別ファイル(`data/pool-3room.json` / `web/pool-3room.js`=`window.NIKENZUME_POOL_3ROOM` / `data/stats-3room.md`)。バッチは roomCount≠2 で接尾辞 `-Nroom` を付ける改修済み。`core.mjs`(生成/solve/episodes/検算)は元々部屋数Nに一般化されており無改修。3部屋は難度が上方シフト(ep0ほぼ無し、山は ep2-4、最大 ep8、手数最大33、解ける率22%・検算失敗0)。気づき分布 ep0:6/1:490/2:940/3:734/4:290/5:98/6:22/7:8/8:1。各サイズ ep1-5 が20問以上で章化可能、ep6(22問)は薄め、ep7-8 は保留。**この3部屋はデータ完成で一区切り**。**残(設計フェーズへ): 3盤レイアウトは縦並び(スマホ前提)に決定、詳細はデザインへ(要件: 3盤を同時に見せる=どの部屋が動いた/止まったか判別できること)。EXTRA章の編成(章=気づき/章内=サイズ→手数 をなぞる。ep0無しなので導入省略)。`web/pool-3room.js` の読み込み**
- **難度の主軸を episodes に確定(2026-06-14、定義改訂 2026-06-25)**: 「ズレ調整エピソード数」= 最短解の中で最小になる、**壁当ての「部屋×方向」パターンが変わるたびに+1** する値(SPEC 2-4)。手数より上位の難度軸。
  - 旧定義(〜2026-06-24): 連続する壁当て手のかたまりを1epとしていた。方向や部屋が変わっても区別せず、体感難度と乖離
  - 新定義(2026-06-25〜): 壁当ての signature(どの部屋がどの方向でブロックされたか)を追跡。同じ部屋で同じ方向に当たり続ければ1ep、方向か部屋が変われば別ep。DP で全最短解から最小 ep を算出。全6626問を再計算、旧 ep=1 の65%が ep=2〜4 に再分類され粒度向上
  - 分布: ep0:234 / 1:1036 / 2:1945 / 3:1700 / 4:1008 / 5:455 / 6:175 / 7:55 / 8:13 / 9:4 / 10:1
  - `tools/core.mjs` solve、`analysis.episodes` に焼き込み済み。**残: 章構成の再設計(通常モード200問+上級モード)・3部屋EXTRA章・問題の目視キュレーション**
- **ルール確定(logicVersion 1.2)**: 同時でないゴール進入は**反則(ミス)**(SPEC.md 1章「ゴール進入制限」)。途中でゴールを通り道に使えない。ソルバーは反則手を解に含めず枝刈り、プレイ側は非同時にゴールへ入ると「行って」から反則判定し**「一手戻す/初形へ」を選ばせる**。core.mjs(遷移/verifySolution)・web/app.js(doMove ミス分岐 + overlay-miss)を更新。プール全再生成済み(2453→1799問)。全1799問で「最終手より前にゴール進入なし」を独立スクリプトで検証、実UIでもミス→一手戻す→正解クリアまで確認
  - 経緯: 一時 1.1 で「ゴール=条件付き壁(スキップ)」案を実装したが、足踏みが妙手に濫用でき意図と違ったため 1.2(反則=ミス)に変更。1.1 は廃止
- **タスクA(プール生成バッチ)完了**: `tools/generate-pool.mjs` + `tools/core.mjs`(依存ゼロのNode)。seeds 1-1000 × 4x4/5x5/6x6 で 1799問生成済み(`data/pool.json`)。スキーマは SPEC.md 4章と完全一致。全問の正解pathを検算してから出力する
- **タスクB(ビューア)初版完了**: `web/`(素のHTML/CSS/JS)。黒基調・発光ボール・移動/壁当てbumpの2アニメ・「詰」演出・切れ目(広告枠プレースホルダ)・章/問題選択・待った/初形・クリア記録(localStorage)・reduced-motion対応。ブラウザ実機で章選択→プレイ→クリア→次問遷移を動作確認済み
- **ヒント・答え実装完了**: 「ヒント」ボタン→下シートで3段(Lv1 壁当ての数 / Lv2 次の一手 / Lv3 答え再生)。SPEC.md「ヒント・答えの設計」参照。Lv2 は**ヒント用の軽量BFSをプレイ側に追加**(`hintFromState` in app.js)し、脇道に逸れた局面からでも次手・残り手数を出す(実UIで脇道局面での復帰を確認)。Lv3 の答え再生は**クリア扱いにせず**初形へ戻す(チート抑制)。広告接続は未実装(機能のみ。方針どおり)
- 統計の副産物 `data/stats.md` あり(サイズ×手数ごとの問題数・解数分布)。SPEC.md 2章の知見(解数中央値2〜3で横並び)をプール実データでも再確認
- 章編成: `web/app.js` の `CHAPTERS` はモード(通常/上級)ごとの**単一章**(`from:0`〜`levels.length`)。レベルは `tools/select-{normal,advanced}.mjs` 生成の固定ID列(`window.NORMAL/ADVANCED_LEVEL_IDS`＋ボスフラグ)から構築。旧「4章×16問・解数フィルタ厳格化」は廃止。
- 正解pathは「データに持つがUIでは隠す」を実装済み。`?debug=1` でのみ「解答」ボタン(再生+妙手ハイライト)が出る

## 次にやるタスク(候補)

0. ~~**盤面の縦並びレイアウト(スマホ優先)**~~ → **2026-06-15 完了・公開済み**(上記「現在地」参照: 2盤を縦1列・画面高合わせで拡大、`--play-chrome`/`--board-gap` で変数化)。**残**: 3部屋プール(`web/pool-3room.js`、2589問・データ完成済み)のビューア統合=**縦並び3盤**。サイズ計算の除数を `/2`→`/3`、`--play-chrome` を3盤用に再調整(チェーン/縦余白)。要件は同じ=3盤を同時に見せ、どの部屋が動いた/止まったか判別できること。
1. **章編成の本決め**: 暫定 `CHAPTERS` を遊んでみて手触りで調整。問題の目視キュレーション(作品性の担保。SPEC.md 3-3)
2. **チュートリアル**: 第一章の前に「壁当て=空振りがズレを作る」を体感させる導入(pathがあるので自動デモも作れる)
3. **ビジュアル磨き込み**: 通り道のマスを照らす表現の強化、駒音SE、タイポグラフィ調整
4. **配信形態の決定**: スマホWebのまま行くか、Flutter等に載せ替えるか(現状の素JSはどちらにも流用可)。**App Store本番の推奨ルート=Capacitor で今の `web/` を WKWebView に包む**(コード書き直し不要・全画面ネイティブ化)→ ビルド/署名は **Mac不要の Codemagic**(運用実績あり)。純PWAはApp Store不可なので、PWA(タスク済)はテスト/ホーム画面配布、本番は要ネイティブラッパー。Flutter移植は代替案だがコスト大。
5. **マネタイズ実装**: `#ad-slot`(切れ目)とリワード(ヒント交換)の実装。SPEC.md 6章

## 未確定で判断保留中の点

- **正解pathをユーザーに見せるか**: データには全問あり。UIは「隠す」実装済みで表示は後付け可能(SPEC.md 4章末尾)
- **章ごとの難度パラメータ表**: `data/stats.md` が根拠資料。`web/app.js` の暫定値からプレイテストで詰める

## 実装上の注意(引き継ぎ)

- `tools/core.mjs` の乱数消費順(壁密度→部屋ごとに[シャッフル→start→goal])を変えると seed 再現が壊れる。変更時は LOGIC_VERSION を上げてプール全再生成
- `web/pool.js` は生成物。手で編集しない。プールを変えたらバッチを再実行
- ソルバーの solution.path は最短解のうち「壁当て手が最多の経路」を採用している(妙手ハイライトの見栄えのため。解数・手数には影響しない)

## 開発者(Den)の前提・好み(共有)

- スタック: Flutter/Firebase(モバイル)、TypeScript/React(Web)、WebGL/GLSL経験あり。2Dグリッドなので技術選択は広い(未確定)
- iOS CI/CD は Mac なしのため Codemagic を使う運用実績あり
- ミニマル/静かなデザイン志向(別アプリ「だれかの読書メモ」でも同方針)
- 長期目標: インディー開発で生活できる収益。マネタイズは本気の論点
